{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:spline-widget": "cd spline-widget && npm install && npm run build",
    "dev:spline-widget": "cd spline-widget && npm install && npm run dev"
  },
  "dependencies": {
    "@coreui/icons": "^3.0.1",
    "@splinetool/react-spline": "^4.1.0",
    "@splinetool/runtime": "^1.12.41",
    "@vercel/analytics": "^1.6.1",
    "sharp": "^0.34.5",
    "vite": "^7.3.1"
  },
  "devDependencies": {
    "terser": "^5.36.0",
    "vite-plugin-compression": "^0.5.1",
    "vite-plugin-image-optimizer": "^1.1.8"
  }
}
